# Decisions & Corrections Log

> Every owner correction or preference from QC rounds gets one line here, newest first. This log is the seed of the context layer — it's what makes output *his*, not generic. Update prompts/conventions from it in the same commit.

- **2026-07-19 (Mayank): SMALL RE-OCR TIER DONE — KIRAN हिन्दी, Lucent हिन्दी, Lucent Computer.** The garble-signature gates (validate_gen.py + query.py, committed same day) closed the student-visible leak for free, but couldn't fix the underlying valid-but-wrong-word class (ठक्राइन, etc.) — only re-OCR does. Real Sarvam cost measured via exact page counts (not estimate): 3 small books = 488 pages = ₹244 (all 6 Sarvam keys freshly topped up same day, no rationing needed). Re-OCR'd, re-ingested (`--replace`), rebuilt passages. **Caught + fixed the known rename trap**: Lucent हिन्दी's OLD un-suffixed book_name (`सामान्य हिन्दी(Lucent)`, no `.pdf`) didn't match the new `.pdf`-suffixed name, so `--replace` inserted 614 new clean passages ALONGSIDE the 349 old garbled ones instead of replacing them — manually purged the 349 old passages + 834 old chunks after confirming via a direct book_name query. Post-fix garble-signature audit (same signatures as the mechanical gates, re-run directly against book_passages): flagged-passage rate dropped from 46%/33%/14% to 0.5%/2%/1% for the three books; zero remaining hits of the severe classes (angle-bracket, Vedic accent, double halant) — residual hits are the noisy orphan-matra signature (mostly page-layout line breaks, not real damage). Passage counts roughly doubled to tripled per book (92->212, 122->304, 349->614) since clean Sarvam extraction recovers far more usable text than the old Tesseract path. **Remaining, deliberately deferred:** the big general-gk tier (Kiran SSC GK, प्राचीन भारत, रमेश सिंह Economy, Laxmikant Polity — 2,967 pages, ~₹1,484) — the mechanical gates already keep it shipping clean (validated 8/8 on live general-gk generation), so this buys retry-efficiency more than correctness; revisit when there's revenue or retry costs visibly bite.
- **2026-07-18 (Mayank): EXAM MODE = OFFICIAL SYLLABUS SEEDS TOPICS; SUBJECT MODE STAYS PYQ-INFERRED. WIRED + BATTLE-TESTED.** Mayank's design, arrived at while scoping the exam-dropdown feature: the PYQ-inferred topic mechanism isn't a bug — it's correct for subject mode (no exam context exists) — but exam mode makes a stronger claim ("this paper mirrors what THIS exam tests") and needs the commission's own syllabus as the topic authority: topic decided first from the official taxonomy, THEN PYQs queried for style; a topic with zero PYQ coverage still generates (this is the freshness edge — syllabus revisions covered the day they publish, years before PYQs exist; also decouples topic variety from PYQ-pool size, critical for exams with thin PYQ sets). **Research (primary sources, sssc.uk.gov.in advertisement PDFs, archived in corpus/syllabus-official/):** UKSSSC runs ONE master syllabus (हिंदी 20 / GK-GS 40 / उत्तराखण्ड 40) across its general exams — Advt 70/2025 (graduate level: Patwari/Lekhpal/VDO/VPDO/ARO/PA/Asst-Supt/Swagati) and Advt 65/2024 (police constable) verified word-for-word identical; Driver (25-mark written + driving test) and Livestock Extension Officer (specialist biology/agri, own build if ever wanted) are genuinely different; Assistant Teacher/Stenographer-skill-test/EO left unsourced per Mayank (low value). **Built:** `DsideOS/worker/syllabus.py` (156 official topics transcribed across 7 corpus subjects), `_extract_topics(…, exam)` official-topic sampling, `police-constable` blueprint family (part weights official, sub-splits derived from group-c measured until his constable papers are ingested), `meta.exam` routing in tasks.py + `exam=` on /api/generate. Live test: 8Q lekhpal-patwari, 8/8 shipped 0 drops, all hand-verified correct, every question on an official topic. **Known polish item (deliberately NOT fixed post-test):** 2/8 hindi stems leaked "पाठ के अनुसार/प्रदत्त सामग्री के अनुसार" into student-facing text — grammar-theory topics surface this SLOT_SYSTEM quirk; fix is a one-line prompt rule but the tuned prompt shouldn't change without re-validation. UKPSC exams: client wants them LATER, explicitly not now.
- **2026-07-17 (Mayank): 3 MORE UNITS TRANSLATED IN PARALLEL from the same Himanshu Sir book — thin subjects strengthened.** Following the uk-geography win (below), ran the same one-time EN->HI `translate_book.py` pass on 3 more units in parallel, each mapped to its correct existing subject (confirmed economy/polity both already fold into `general-gk` per corpus convention, not separate buckets — checked via a live DB query before assuming): **Unit 3 Environment/Ecology (pp214-318) -> uk-geography** (extends the same subject, forest cover/flora-fauna/biodiversity — 307 chunks -> 175 passages); **Unit 5 Society/Culture (pp453-485) -> uk-culture**, the thinnest culture-adjacent unit (87 chunks -> 49 passages); **Unit 7 Policies/Programs (pp578-620) -> uk-general-studies**, the overall thinnest subject (126 chunks -> 76 passages). Deliberately held back Unit 1 History and Unit 4/6 Economy/Polity — those map to already-strong subjects (uk-history 3651, general-gk 8247), lowest marginal value; Unit 8 District Study (219pp) needs subject-splitting logic per district, not a blind dump — parked for a future pass. All 3 spot-checked against English source (Yarsa Gumba/कीड़ा जड़ी fungus details, Jitu Bagdwal ballad era ~1591-1610 AD, CM Helpline exact launch date 23 Feb 2019 — all survived translation intact) and live-retrieval-verified. `corpus_health.py`: 0 flagged across all 27 sources. Corpus now 27 sources total (24 after the geography win + these 3). The rest of the 840-page book (History, Economy, Polity, District-study units) remains a known, reusable asset at ~₹10-15/unit whenever those subjects need strengthening.
- **2026-07-17 (Mayank): uk-geography FACT-GAP CLOSED via one-time English->Hindi translation — new `translate_book.py` tool.** uk-geography had 113 PYQ style chunks but ZERO fact-passages (the corpus folder was empty — never sourced). No free/comprehensive Hindi-native Uttarakhand-geography book could be found (Jardhari/Sharda Pustak Bhawan have the right content but are paywalled with no fetchable file; UoU's GEOG-604 SLM is free but English AND generic/padded prose, rejected). Found "A Comprehensive Study of Uttarakhand" (Himanshu Sir, 840pp, clean digital text layer, fact-dense — exact pass elevations, sourced census/forest-cover tables) — but English. Built a **one-time OFFLINE translation step** (`rag/translate_book.py`) rather than a live pipeline stage: translating English source text once, upfront, keeps `book_passages` Hindi-only like every other source, avoiding two live risks — English-register bleeding into Sarvam's Hindi drafting, and the lexical/BM25 half of hybrid retrieval never firing on English passages (queries are Hindi tokens). Extracted ONLY Unit 2 (Geography, pp88-213) to keep subject-tagging clean (ingest.py tags subject = folder name, so mixing units would mislabel history/economy/culture chunks as uk-geography) — rest of the book (history/economy/culture/polity units + Unit 3 ecology) is a **known, deliberately parked asset** for strengthening other thin subjects later at the same ~₹10/unit cost. Translated via gpt-5.4-nano, chunked+resumable (`.translate/` checkpoints, gitignored like `.reocr/`/`.checkpoints/` — copyrighted book text, repo is public). **Measured cost: ~₹10 for the whole unit** (53.5k input + 86k output tokens) — cheaper than Sarvam re-OCR would have been, since the clean text layer skips OCR entirely. Spot-checked against the English source: exact pass elevations (Mana 5611m, Niti 5068m, Shalshal 4496m, Kungribingri 5578m, Lampya 5532m, Lipulekh 5453m) and all place/tribe names survived translation perfectly. Ingested: 396 chunks -> 210 passages, `corpus_health.py` clean (0 flagged in `book_passages`). Live retrieval verified working on 3 real geography topics (passes, drainage, Bhotiya tribe) before calling this done. Corpus now 24 sources.
- **2026-07-16 (Mayank): GRAMMAR = MODEL-REASONING + PYQ-STYLE-BANK, NOT FACT-CORPUS. LOCKED (revised same day).** Hindi-grammar questions (विलोम, संधि, समास, पर्यायवाची, वाक्य-शुद्धि, etc.) do NOT belong in `book_passages` (the fact-grounding pool) — same reasoning as maths/reasoning: the answer is language COMPETENCE the model already has natively (Sarvam is Hindi-native), not a fact stored in a passage; grounding रुल-application against a corpus is useless and actively harmful (list/pair content chunks into unretrievable blobs — this is what made हरदेव बाहरी and hindi_vyakaran look "bad" on inspection, when the CONTENT was fine, just wrongly placed). **Correction from the first pass same day:** initially called this "zero corpus, like maths" — too broad. Reviewing hindi_vyakaran_sampurn_notes_mcq_jardhari showed it's actually a clean, exam-shaped MCQ bank (Q+4 options+marked answer, संधि-विच्छेद etc.) — that's a **style/PYQ example**, not a fact source. Grammar's correct shape: **model supplies the rule-reasoning (like maths); a PYQ-style bank supplies the question PATTERNS/phrasing/difficulty to imitate (like `pyq_chunks` already does for other subjects)** — never `book_passages`. **Action taken:** both हरदेव बाहरी (word-list dictionary, no question shape — 80 passages/1834 chunks) AND hindi_vyakaran (MCQ bank, right content/wrong table — 131 passages/334 chunks) REMOVED from `book_passages`/`book_chunks`. Corpus now 23 sources, 0 flagged. **TODO (deferred, real design work):** (1) build the grammar generation path — rule-reasoning prompt, grounding gate DISABLED (maths-style self-verification, or a light second-model check — undecided); (2) route hindi_vyakaran (and similarly-shaped grammar MCQ books) into the PYQ/style-example pool instead of re-ingesting into book_passages; (3) re-evaluate remaining hindi books (KIRAN सामान्य हिन्दी, Lucent, aditya vastunisth) — same fact-vs-grammar split applies.
- **2026-07-15 (Mayank): SARVAM RE-OCR IS MANDATORY FOR EVERY NEW BOOK — LOCKED.** From now on, no book enters the corpus through the local text-layer/Tesseract path. Every new book is Sarvam-digitized first (`reocr_book.py --book <path>`), then ingested from its clean `.reocr/<stem>.full.md` sidecar. Reason: Sarvam reads the rendered page image, so it is immune to the entire class of extraction failures we keep hitting — Kruti Dev *and* other legacy Hindi fonts (Chanakya/APS/Shusha) that display perfectly but extract as Latin gibberish, Tesseract digit corruption (1→4/7), and matra-garble. Triggered by the YCT "1,00,000 GS Pointer" book: excellent one-liner content, but a non-Kruti-Dev legacy font that neither `is_krutidev` nor the KD converter could rescue (`is_krutidev`=False, forced conversion still garbage). Pairs with Mayank's corpus philosophy: **quality > quantity — 3-4 great books per subject beats 1000s of mediocre ones** (explicit disagreement with the owner's "put 1000s of books in the DB"). The local text-layer/Tesseract/Kruti-Dev branches in `ingest.py` are now LEGACY fallback (for books ingested before this rule), not the path for new books. Rule also locked in `rag/ingest.py` docstring.
- **2026-06-14 (Mayank): DIAGRAM EMBEDDING + ANSWER-KEY PDF + TOPIC-AWARE SOURCE TIERS.** Three additions for the live run: (1) **Diagram questions** (reasoning/maths with figures) handled hybrid — text parts stay branded (Kruti Dev/template), diagram parts cropped **verbatim** from the rendered paper image via `pipeline/crop_figure.py` into `review/input/crops/`. JSON gains `"image"` (a question's own figure) and `"option_images"` (when the a–d options are themselves diagrams). All three generators embed them; `run_pipeline.py` refuses if any referenced crop is missing. Figure sizes locked COMPACT after owner feedback (Word doc was spilling pages): stem 4.8×4.0cm, options 3.6×3.0cm (capped by width AND height); solution doc 5cm; PPT auto-fits one slide. (2) **Answer-key PDF** — owner asked for a simple one-page उत्तरमाला alongside other outputs. `pipeline/build_answer_key.py` (PyMuPDF + Nirmala), branded header, `N. (x)` grid scaling to 5 cols for 100 Qs. Pipeline now emits **4 artifacts**. (3) **Answer-verification revised to TYPE- and TOPIC-aware:** reasoning/simple-maths are **self-verified** (worked `solution` = proof, no external check); factual questions need two-source confirmation split by topic — **national GK** → Testbook + ExamPillar; **Uttarakhand-specific GK** (~40-50% of a UKSSSC paper) → ExamPillar UK section + one UK site (uttarakhandexamsgk.com / studyfry.com / gktoday.in). Reason: Testbook/ExamPillar skew national, thin on UK-specific facts (Mayank flagged). PIB/india.gov.in = current-affairs facts, not answer keys.
- **2026-06-14 (Mayank): SOLUTION WORKFLOW + ANSWER-VERIFICATION + PIPELINE SKILL LOCKED.** Three decisions in one session: (1) **Solution doc = `build_solution.py` with reasoning**, confirmed against the dry-run doc that carries `संकेत:`/`हल:` per question (the answers-only version, where the JSON had no `reason` field, was rejected). (2) **Answer-verification source hierarchy for live papers** (no official key yet): **Tier 1 = ExamPillar + Testbook — both must agree to ship**; one-source/disagree/none → RED ⚠ flag for Mayank's manual check; Tier 2 fallback = Adda247 / NCERT corpus / worked solution; PIB & india.gov.in are current-affairs *fact* sources, NOT answer keys. **Manual-review flags + source provenance render in the TEACHER solution doc ONLY** — never the branded PPT/paper. PYQs with the commission's official key set `"answer_source":"official_key"` → no flags, green "✔ स्रोत: आधिकारिक उत्तर कुंजी" line. (3) **`/run-pipeline` skill + `pipeline/run_pipeline.py` orchestrator built** — single entry point; HARD INPUT GATE: refuses unless exactly one valid questions JSON sits in `review/input/`; outputs all three artifacts to `review/output/`. Validated end-to-end on the real UKSSSC Patwari/Lekhpal 12-Feb-2023 paper (100 Qs, official key, 0 false flags). **Still manual via Claude Code — no deployment.** `run_pipeline.py` is the productization seam: API vision/reason calls slot in ahead of it, the three generators never change.
- **2026-06-12 (owner, post-demo): expansion pull = content creation + MARKETING engine.** After seeing the demo he named marketing as his next interest — consistent with discovery ("kept insisting on content/marketing help"). Mayank's read: true for most Indian edu owners. **Sequencing decision (Mayank): lock the current content workflow in first (trial → retainer), marketing engine comes next** — ahead of customer service in the §07 ladder for this client, because the client is pulling. Caveat from discovery still stands: his real growth channel is word-of-mouth, so the marketing build should lean results/testimonials/referral-engine, not generic social posting.
- **2026-06-11 (Mayank): PAPER PIPELINE STRUCTURE LOCKED — production level.** Demo paper (20 original UK-history questions, his full template: front page, borders, watermark, Kruti Dev, उत्तरमाला) approved by Mayank and sent to the owner. The flow `questions JSON → build_paper.py → branded .docx` is now the production process. **Operation stays MANUAL inside Claude Code — no hosting, no server, no deployment, nothing productized yet.** Changes only when Mayank says so. Output files get client-facing names (e.g. "UK History Practice Paper - Target Academy.docx"), never internal spike names.
- **2026-06-11 (Mayank, direction-setting):** do **NOT** replicate the institute's borrow-questions flow (books + ad-hoc ChatGPT/Gemini). We generate **original questions** from a strong grounding corpus whose backbone is **PYQs (UKSSSC previous-year papers)** — they define real exam pattern, difficulty, and topic weightage. His papers + book material (photos/scans as needed) supply facts; PYQs supply calibration. **Anthropic API use authorized for question generation from the PYQ corpus** (carve-out from the manual-first policy).
- **2026-06-11** — Owner prefers **Kruti Dev** font for all material (via Mayank). Drives the Unicode↔KrutiDev conversion requirement in both `deck.py`/`paper.py` (output) and `ingest.py` (corpus parsing). See `format-conventions.md` §PPT.
