# Anthropic SDK: 0.111+ required for claude-sonnet-4-6, vision blocks, and the
# web_search tool used by extract_vision.py / verify_answers_ai.py. The old
# 0.25.x predates all three and silently breaks the --live paths.
anthropic>=0.111.0
openai>=2.0.0

# Document + image pipeline (builders and extractors)
python-docx>=1.1.0
PyMuPDF>=1.24.0
Pillow>=10.0.0

# Scraping / search (fetch_questions, scrape, search)
playwright>=1.44.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0

# Referral QR on papers/decks (referral_qr.py): every document a student
# receives becomes a self-registration point for the referral program.
qrcode>=7.4
