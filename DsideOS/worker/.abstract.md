Celery worker: the 5 pipeline tasks (extract/build/answer-key/solutions/full) with format-agnostic input routing, disk-backed job storage, hourly cleanup beat, and optional Phoenix tracing (tracing.py, off by default).
