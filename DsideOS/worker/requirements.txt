# Job runtime
celery>=5.3.0
redis>=5.0.0
pydantic-settings>=2.0.0

# LLM — OpenAI primary (text), Anthropic fallback + vision. See pipeline/llm.py.
openai>=2.0.0
anthropic>=0.111.0

# Document + image pipeline (builders + extractors)
python-docx>=1.1.0
python-pptx>=0.6.23
PyMuPDF>=1.24.0
Pillow>=10.0.0

# NOTE: .docx -> .pdf on the server uses LibreOffice headless (installed in the
# Docker image), NOT pywin32. The Kruti Dev 010 + Nirmala UI fonts must be
# installed system-wide in the image for correct PDF rendering.
